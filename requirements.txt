# CS2 YOLO Bot Dependencies
# Install with: pip install -r requirements.txt

# YOLOv8 framework
ultralytics>=8.0.0

# PyTorch with CUDA (for GPU acceleration)
# Note: Install the correct CUDA version for your system from https://pytorch.org
torch>=2.0.0
torchvision>=0.15.0

# Image processing
opencv-python>=4.8.0

# Fast screen capture (Windows only)
mss>=9.0.0

# Array operations
numpy>=1.24.0

# Mouse/keyboard input simulation (Windows only)
pydirectinput>=1.0.4
pywin32>=306

# Global hotkey listener
pynput>=1.7.6
